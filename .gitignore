# secrets
.env

# local env / caches
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store

# vendored upstream (re-clone via scripts/setup.sh)
vendor_kronos/

# data artifacts — regenerated by the pipeline
cache/
*.db
*.db-journal
notebooks/_out/

# NOTE: log/{forecasts,watchlist,actuals}.jsonl and memory/{episodes.jsonl,stats.json}
# are the immutable desk record and ARE tracked (git history = the tamper-evidence).
# desk.db is a local queryable mirror rebuilt from them, so ignored.

# model weights land in the HF cache, not the repo
hf_cache/

# run logs (not the record)
log/launchd.*
log/*.log
log/*.out
log/*.err
_ablation/
