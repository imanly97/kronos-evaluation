# local env / caches
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
.pytest_cache/

# vendored upstream (re-clone via scripts/setup.sh)
vendor_kronos/

# model weights
hf_cache/

# price cache — regenerated by src/data.py
cache/

# forecast + metric store — regenerated by src/forecast.py / evaluate.py
# (kept OUT of git while iterating; a frozen copy is committed once dev is locked)
store/

# notebook scratch output
notebooks/_out/

# secrets (none needed for the eval, but just in case)
.env
