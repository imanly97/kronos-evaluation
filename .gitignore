# secrets
.env

# local env / caches
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store

# vendored upstream (re-clone via scripts/setup.sh)
vendor_kronos/

# data artifacts — regenerated by the pipeline
cache/
*.db
*.db-journal
notebooks/_out/

# NOTE: log/*.jsonl is the immutable signal log and IS tracked (git history = the
# tamper-evidence). narb.db is a local queryable mirror rebuilt from it, so ignored.

# model weights land in the HF cache, not the repo
hf_cache/

# launchd logs
log/launchd.*
