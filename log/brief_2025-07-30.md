# Morning brief — 2025-07-30

Triage kept **7** of 7 candidates examined (universe 40).

## JPM  ·  momentum-breakout  ·  rank 1

JPM tops the flag list on model strength (z≈2.18 per-name), set up as a momentum-breakout: price near the top of its range (range_pos 0.93), just 1.4% off the recent high, 20% above the recent low, on a modest positive trend (slope 0.0017, r² 0.36) with a small unfilled gap of 0.58%. Volume is actually light relative to its longer average (rvol ratio 0.77), so this isn't a volume-confirmed breakout. The model's distribution leans constructive — median +1.3%, p_up 0.95 — with modest dispersion (0.9%). The analog sample is sufficient: 45 prior momentum-breakout instances split close to a coin flip, 51% up, mean +0.11%, average move 0.78%, so historical edge is thin relative to the model's stated confidence. The name sits in a highly correlated cluster (mean |corr| 0.75 across five peers) but same-sign agreement is only 60%, reading as cluster-aligned rather than a clean idiosyncratic move. Desk memory is essentially empty — one prior episode, which missed direction — too small to draw any track-record conclusion from. Caveat: the analog base rate is near 50/50 and desk memory has only one (wrong-direction) prior instance, so there's little empirical backing behind the model's confidence.

_groundedness 100% over 1 critic round(s)_

---

## META  ·  range-bound  ·  rank 2

META lands on the flagged list at rank 2, driven by a model strength read (z 2.25) well outside the usual range, with a median forecast return of 1.52% and a notably high probability-up estimate of 0.89. The setup itself is unremarkable on its face: price is range-bound, sitting in the upper part of its recent range (range_pos 0.78), with short-term realized vol running below its longer window (ratio 0.70) and only a small stretch from the moving average — quiet, coiled tape rather than a trending one. The analog base rate does not support the bullish skew: 25 prior range-bound instances split roughly evenly (48% up) with a slightly negative mean return of -0.52% and average absolute move of 1.83%, closer to a coin flip than a confident lean. The cluster read is aligned rather than idiosyncratic — three peer names all show same-sign moves with average absolute correlation of 0.61, suggesting part of this signal may be sector/macro-driven rather than META-specific. There is no desk memory to lean on, as this is the first tracked range-bound episode for the name. Caveat: the model's bullish tilt is not corroborated by the analog base rate, which sits close to neutral.

_groundedness 100% over 1 critic round(s)_

---

## BAC  ·  quiet  ·  rank 3

BAC screens near the top of the flagged list on model strength (z ~2.57), with a median expected move of +0.89% and a notably high probability of an up day (89%), against fairly tight dispersion (5–95% range roughly -0.6% to +2.1%). The setup itself is "quiet" — flat trend, low realized vol relative to its longer-run average, sitting near the top of its recent range (~87th percentile), with only a modest gap and stretch from moving average. That's the flag: strong model conviction layered on an otherwise unremarkable, low-volatility tape. History for this exact pattern is mixed — 42 prior quiet instances split close to even (45% up) with a slightly negative average return, so the base rate doesn't obviously support the current skew. The cluster read leans aligned rather than idiosyncratic: average absolute correlation to peers is 0.71 with 67% same-sign behavior, suggesting this may be more a sector-wide move than something specific to BAC. Desk memory has no prior quiet-setup episodes for BAC to lean on. Caveat: the analog base rate is roughly coin-flip and slightly negative on average, which sits in tension with the model's high conviction and up-probability.

_groundedness 100% over 1 critic round(s)_

---

## MA  ·  range-bound  ·  rank 4

MA lands on the flag list with an 86% probability of a positive next-day return and a strength reading well above typical, notably stronger than the setup label alone would suggest. The setup itself reads as unremarkable: price sits roughly mid-range (range position 0.55), trend is flat, price is 5.4% below its recent high and 6.5% above its recent low, with short-term realized vol running well under longer-term vol (ratio 0.43) — a compressed, directionless tape. The analog sample is sufficient (33 prior range-bound instances) and skews constructive: 70% up-days, mean return +0.38%, average absolute move 0.64%. The cluster read is aligned rather than idiosyncratic — two peers move with MA in the same direction 100% of the time, with a mean absolute correlation of 0.76, suggesting the signal is shared across related names rather than isolated to MA. There is no desk memory to draw on here, as this setup has zero prior tracked episodes for MA. Caveat: with no historical hit-rate for this specific signal on MA, confidence rests entirely on the analog sample and a two-peer cluster read, not on any name-specific track record.

_groundedness 100% over 3 critic round(s)_

---

## DIS  ·  quiet  ·  rank 5

DIS lands on the flag list mainly on strength of the model's forecast skew: median return of +0.75% with 84% probability of an up move, and a strength_z of 1.2, notably higher conviction than the price action would suggest. The setup itself is "quiet" — trend is flat-to-slightly-down (slope -0.0009, weak r2), realized vol has compressed versus its longer window (rvol ratio ~0.51), and price sits high in its recent range (86.9th percentile) with a small gap down from yesterday. In short: a sleepy tape with a model that's leaning firmly bullish. The analog base rate pushes back on that lean — 41 prior quiet instances split almost evenly (46% up, mean -0.26%, average absolute move ~1.15%), so history doesn't offer strong confirmation. Cluster read is idiosyncratic, no correlated peers driving this. Desk memory is empty — zero prior quiet-setup episodes logged for DIS, so there's no track record to lean on either way. Caveat: the analog sample shows a near coin-flip base rate that sits well below the model's 84% up-probability, and there's no desk history to validate the signal.

_groundedness 100% over 0 critic round(s)_

---

## UNH  ·  post-gap-drift  ·  rank 6

UNH lands on the flag list after another gap lower, extending a downtrend that's been notably persistent (trend r² above 0.84) rather than choppy. The setup is a post-gap-drift: price gapped down 5.8% and the model is watching for continuation or fade in the day(s) after, with the stock now sitting deep off its highs (-60%) and near the low end of its recent range. The model's own read leans negative — median forecast -2.68%, with only about a 20% chance of a positive close and a fairly wide dispersion (~3.1%), so the distribution skews down but isn't tight. Analog history is technically sufficient (8 prior instances) but not very informative: base rate up was a coin-flip 50%, mean next-day return near flat (+0.17%), with average absolute moves of 2.4% — so the historical edge here is thin. The cluster read shows this as idiosyncratic, not part of a broader correlated sector or macro move, with no peer overlap flagged. Desk memory is empty — there's no track record yet for UNH in this setup to lean on. Caveat: the analog sample is small and shows no real directional edge, so the model's negative skew is not well corroborated by historical precedent.

_groundedness 100% over 0 critic round(s)_

---

## V  ·  range-bound  ·  rank 7

V flagged with a notably strong model read (strength_z 1.95) despite sitting in a fairly quiet, range-bound tape — price is roughly mid-range (range position 0.43), trend is essentially flat (slope -0.0004, r2 0.061), and short-term volatility has cooled well below its longer-run level (rvol ratio 0.64). The model leans clearly bullish, with median return of +0.72%, 81.5% odds of an up day, and a 90% band spanning roughly -0.6% to +2.1%. History offers reasonable support: 34 prior range-bound instances for V show a 65% up rate and mean return of +0.38%, so the current forecast runs a bit richer than the historical average but points the same direction. The cluster read is aligned rather than idiosyncratic — V's single close peer, MA, carries a 0.95 correlation with matching sign, suggesting this is more of a shared payments-sector signal than a name-specific catalyst. There is no desk memory to lean on here — zero prior tracked episodes for V in this setup, so there's no hit-rate history to weigh. Caveat: the bullish read is amplified by a highly correlated single-peer cluster rather than independent confirmation, and desk track record for this exact setup is nonexistent.

_groundedness 100% over 0 critic round(s)_

---
