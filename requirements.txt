# Kronos evaluation — pinned deps for re-running the notebooks / probe scripts.
# Installed into a project-local .venv (see README) so the anaconda base env
# and its jupyter stay untouched.

# --- core numerics ---
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1

# --- Kronos (frozen forecaster) ---
torch>=2.2
einops==0.8.1
huggingface_hub==0.33.1
safetensors==0.6.2
tqdm==4.67.1

# --- prices ---
# Yahoo's chart API directly through curl_cffi (browser-TLS impersonation) —
# the stdlib/requests UA gets 429'd.
curl_cffi==0.7.4

# --- charts + notebooks ---
matplotlib==3.9.3
jupyterlab==4.3.4
ipykernel==6.29.5
nbconvert==7.16.4
